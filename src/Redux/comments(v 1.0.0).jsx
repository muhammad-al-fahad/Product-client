import { COMMENTS } from '../Shared/comments(v 1.0.0)';

export const Comments = (state = COMMENTS, action) => {
    switch (action.type) {

        default:
          return state;
      }
};
