import { PROMOTIONS } from '../Shared/promotions(v 1.0.0)';

export const Promotions = (state = PROMOTIONS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};
